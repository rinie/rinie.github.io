Collecting some old thoughts about **Pace Layering**, **Train-ing**, **Def-Use** and **Gutenberg** model
You don't print a document, paragraph or chapter, you print a range of pages.
So the mapping to pages should be handled in the semantic layer.
But the Gutenberg layer is based on pages.

Unix, TCP/IP and Virtual memory add **bytestream** and page size between 512 and 4096 bytes as a 2.0 version.
UTF-8 and git add 2.1. UTF-8 shows the O(1) pagebreak versus Unbounded context of XML or BOM
Claude added **DNS** as **external resolver** distinction. TCP/IP split between Name (Semantic) and IP adresses (Gutenberg).
**URI** or **XML/C++** namespaces with internal resolver means hardcoded, like hardcoded IP adresses or MAC adresses.
So **URL** supports ***external resolver** **DNS** and **HTTP(S)** redirect so that is Gutenberg, URI/URN are hardcoded Semantic
npm or cargo provide a DNS like mapping between your code and the used libraries

Some rants as these are generated by brainstorming with Claude.
But Claude recognizes the context without having my old notes.
Amazing.

Add a [page 15](https://page15.org/about/our-story/) link?
